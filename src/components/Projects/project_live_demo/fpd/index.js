/**
 * Finance Payroll Department (FPD) Module
 * 
 * This module contains all components, sections, and utilities
 * for the Finance Payroll Department interface.
 * 
 * Structure:
 * - sections/   : Main page sections (Dashboard, SalesInvoice, etc.)
 * - components/ : Reusable UI components (DataTable, MenuButton, etc.)
 * - forms/      : Form configurations (SaleInvoiceForm, CustomerForm)
 * - ui/         : UI primitives (Input, Select, WizardModal, etc.)
 */

// Sections
export * from './sections'

// Components
export * from './components'
